#!/bin/bash

# pull all repos
# - secrets if running as a user
# - dev-install

# destroy environment if it exists

# create server

# wait for SSH to be available

# decrypt "openshift" user password

# run dev-install with config
